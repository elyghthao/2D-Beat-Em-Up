using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.TestTools;

public class StatsTest
{
    // [UnityTest]
    // public IEnumerator CheckPlayerStats() {
    //     // string[] scenes = {"Scenes/MainScenes/Level_1", "Scenes/MainScenes/Level_2", "Scenes/MainScenes/Level_3"};
    //     // AsyncOperation sceneLoader = SceneManager.LoadSceneAsync(scenes[0]);
    //     // while (!sceneLoader.isDone) yield return null;
    //     // GameObject player = GameObject.FindWithTag("Player");
    //     //
    //     //
    //     // sceneLoader = SceneManager.LoadSceneAsync(scenes[1]);
    //     // while (!sceneLoader.isDone) yield return null;
    //     // player = GameObject.FindGameObjectsWithTag("Player");
    //     // Assert.IsTrue(player.Length == 1, "Checking if there is one object with the player tag in " + scenes[1]
    //     //     + ". \nFound: " + player + " in array length of " + player.Length);
    //     //
    //     // sceneLoader = SceneManager.LoadSceneAsync(scenes[2]);
    //     // while (!sceneLoader.isDone) yield return null;
    //     // player = GameObject.FindGameObjectsWithTag("Player");
    //     // Assert.IsTrue(player.Length == 1, "Checking if there is one object with the player tag in " + scenes[2]
    //     //     + ". \nFound: " + player + " in array length of " + player.Length);
    //     Assert.IsTrue(true);
    //     
    // }

    // public void PlayerStats(GameObject p) {
    //     var pStateMachine = p.GetComponent<PlayerStateMachine>();
    // }
}
